export enum Route {
  Api = 'api',
  Cv = 'cv',
  Education = 'education',
  Employment = 'employment',
  Project = 'project',
  Technology = 'technology',
  Contact = 'contact',
}

export enum ErrorMessage {
  CantWrite = 'Cant write file',
}

export enum Field {
  Id = 'id',
  Icon = 'icon',
}
